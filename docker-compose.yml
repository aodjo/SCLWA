version: '3.8'

services:
  c-runner:
    build: .
    image: c-tutor-runner
    volumes:
      - ./temp:/code
    network_mode: none
    mem_limit: 128m
    cpus: 0.5
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=32m
